html 
    head 
        title Change password 
    body
    form(action="/updatepass" method="POST")
        h3 Change your password here!
        div 
            label(for="currentpass") Current Password: 
            input(name="currentpass" type="password" required)
        br
        div 
            label(for="newpass") New Password: 
            input(name="newpass" type="password" required)
        br
        div 
            label(for="repeatpass") Repeat Password: 
            input(name="repeatpass" type="password" required)
        br
        button(type="Submit") Submit  